# FinSage Python dependencies
# Install with:  pip install -r requirements.txt
# ---------------------------------------------------------------------------
# Core Databricks / Spark
databricks-sdk>=0.20.0
pyspark>=3.5.0

# ---------------------------------------------------------------------------
# Data ingestion
sec-edgar-downloader>=5.0.0
requests>=2.31.0

# ---------------------------------------------------------------------------
# Data processing
pandas>=2.0.0

# ---------------------------------------------------------------------------
# NLP / vector search
tiktoken>=0.6.0
langchain>=0.1.0
langchain-text-splitters>=0.0.1

# ---------------------------------------------------------------------------
# SEC filing parsing (Silver section extractor)
# DOM-aware iXBRL / HTML parser — replaces the legacy regex extractor in
# 03_silver_decoder.py. Handles HTML entities, inline-XBRL span fragmentation,
# and page-header/footer removal natively.
sec-parser>=0.58.0
lxml>=5.0.0

# ---------------------------------------------------------------------------
# PDF / document parsing
pypdf>=4.0.0
pdfplumber>=0.10.0

# ---------------------------------------------------------------------------
# Configuration
python-dotenv>=1.0.0

# ---------------------------------------------------------------------------
# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
