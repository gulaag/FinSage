"""FinSage evaluation package.

Modules:
    scorers     — MLflow GenAI scorers (custom + curated wrappers around built-ins)
    persistence — Delta-table writers/readers for eval result history
    analysis    — Failure breakdown and cross-run regression utilities
    builder     — Ground-truth dataset generator (separate package)
"""
